require "rails_helper"

describe UnregisterNotificationsJob do

  # let(:registration_id) { "wsadsafghwwww" }
  #
  # describe "perform" do
  #   context "google" do
  #     it "push google" do
  #       expect(GoogleMessaging).to receive(:unregister).with(registration_id)
  #
  #       subject.perform(registration_id, 0)
  #     end
  #   end
  #
  #   context "google" do
  #     it "push osx" do
  #       expect(AppleNotifications).to receive(:unregister).with(registration_id)
  #
  #       subject.perform(registration_id, 1)
  #     end
  #   end
  # end
end
